[package]
name = "lacs-types"
version = "0.1.0"
edition = "2021"

[dependencies]
lacs-proto = { path = "../lacs-proto" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
