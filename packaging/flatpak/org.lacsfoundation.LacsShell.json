{
  "app-id": "org.lacsfoundation.LacsShell",
  "runtime": "org.gnome.Platform",
  "runtime-version": "47",
  "sdk": "org.gnome.Sdk",
  "sdk-extensions": ["org.freedesktop.Sdk.Extension.rust-stable"],
  "command": "lacs-shell",
  "finish-args": [
    "--share=ipc",
    "--socket=fallback-x11",
    "--socket=wayland",
    "--device=dri",
    "--share=network",
    "--filesystem=/run/lacs:ro",
    "--env=LACS_LISTEN_URI=unix:///run/lacs/daemon.sock"
  ],
  "build-options": {
    "append-path": "/usr/lib/sdk/rust-stable/bin",
    "env": {
      "CARGO_HOME": "/run/build/lacs-shell/.cargo",
      "RUSTFLAGS": ""
    }
  },
  "modules": [
    {
      "name": "lacs-shell",
      "buildsystem": "simple",
      "build-commands": [
        "cargo build --release --locked -p lacs-shell",
        "install -Dm755 target/release/lacs-shell /app/bin/lacs-shell"
      ],
      "sources": [
        {
          "type": "dir",
          "path": "../../"
        },
        {
          "type": "file",
          "path": "cargo-sources.json",
          "dest": ".cargo",
          "dest-filename": "config.toml"
        }
      ]
    }
  ]
}
